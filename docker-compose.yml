version: "3"

services:

  app:
    image: node:dubnium-alpine
    volumes:
      - ./:/app
    working_dir: /app
    environment:
      NODE_ENV: development
    ports:
      - 5010:5010
    command: npm start